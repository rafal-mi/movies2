<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/tools">

    <data>
<!--        <import type="org.example.movies.R" />-->
        <variable
            name="viewModel"
            type="org.example.movies.MainViewModel"
            />

        <variable
            name="item"
            type="org.example.movies.data.db.Movie"/>

        <variable
            name="position"
            type="int"/>

    </data>
    
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:layout_margin="4dp"
        card_view:cardCornerRadius="16dp"
        card_view:cardBackgroundColor="#4f4f4f"
        >
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            >
            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:textStyle="bold"
                android:text="@{@string/item_id(item.id)}"
                >
            </TextView>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:text="@{item.originalTitle}"
                >
            </TextView>

            <ImageView
                android:id="@+id/play_image_view"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_baseline_favorite_24"
                android:onClick="@{() -> viewModel.mark(item, position)}"
                app:tint="@{item.favorite ? @color/purple_200 : @color/teal_200}">

            </ImageView>

        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>